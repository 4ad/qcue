Quantified syntax and its desugared AST.

-- in.cue --
x: {
exists T: U
forall V: W
value: T
transform: func(V) -> V
}
-- debug.txt --
x: exists (T: U) forall (V: W) {value: T, transform: func(V) -> V}
