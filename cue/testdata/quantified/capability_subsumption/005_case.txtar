Quantified CUE: CapabilitySubsumption/forall (A: int) func(A) -> A / forall B func(B) -> B

-- in.cue --
@test(validate)
@test(subsume, a, b)

a: forall (A: int) func(A) -> A
b: forall B func(B) -> B
