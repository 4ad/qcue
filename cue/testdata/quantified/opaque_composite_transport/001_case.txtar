-- in.cue --
@test(json, [2,3], at="out")

Box(A) = [...A]
#I: exists (A) {seed: A, box: Box(A), read: func(A) -> int}
p: seal #I with (A = int) {
	seed: 1
	box:  [2, 3]
	read: func(x: int) -> int: x
}
out: (open p as (T, P) {out: [P.read(P.box[0]), P.read(P.box[1])]}).out
