-- in.cue --
@test(json, 3, at="out")

Box(A) = {value?: A}
#I: exists (A) {seed: A, box: Box(A), read: func(A) -> int}
p: seal #I with (A = int) {
	seed: 1
	box:  {value: 3}
	read: func(x: int) -> int: x
}
out: (open p as (T, P) {out: P.read(P.box.value)}).out
