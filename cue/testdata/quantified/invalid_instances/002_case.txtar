#inlinetest:exclude Formation errors use golden compiler diagnostics.

-- in.cue --
id:  forall (A in int) func(x: int) -> int: x
out: id(1)
-- out/evalalpha --
id: value binders require the dependent profile D:
    ./in.cue:1:14
