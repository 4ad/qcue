// An established contradiction remains an error with or without a
// concreteness demand; it must not be downgraded to an incomplete proof.

-- in.cue --
@test(validate, conflict, at="f")
@test(validate, concrete, conflict, at="f")
f: func(int) -> string
f: func(x: int) -> int: x
