// Paper example 061: Indexed finite sequences

#noformat
#paper: 61
#mode: evaluation
#inlinetest:exclude Not an executable S_H/A example; see TestQuantifiedPaperIndex.
#skip
Dependent profile D (out of scope).

-- paper.cue.txt --
import "list"

let Nat = int & >=0
Vec(A, n in Nat) = [...A] &
    list.MinItems(n) & list.MaxItems(n)
Fin(n in Nat) = int & >=0 & <n
