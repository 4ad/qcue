// Paper example 022: Full symmetric, higher-rank CUE: S_H

#noformat
#paper: 22
#mode: evaluation

-- paper.cue.txt --
use: func(forall A func(A) -> A) -> [int, string]

#Showable: exists A {
    value: A
    show:  func(A) -> string
}
show: func(#Showable) -> string
-- in.cue --
@test(validate, at="use")

use: func(forall A func(A) -> A) -> [int, string]

#Showable: exists A {
    value: A
    show:  func(A) -> string
}
show: func(#Showable) -> string
