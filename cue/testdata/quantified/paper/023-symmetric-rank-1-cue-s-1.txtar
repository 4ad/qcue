Paper example 023: Symmetric, rank-1 CUE: S_1

#paper: 23
#mode: evaluation

-- paper.cue.txt --
Counter: exists State {
    zero: State
    next: func(State) -> State
    read: func(State) -> int
}
-- in.cue --
@test(validate, at="Counter")

Counter: exists State {
    zero: State
    next: func(State) -> State
    read: func(State) -> int
}
