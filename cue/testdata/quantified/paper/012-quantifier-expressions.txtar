// Paper example 012: Quantifier expressions

#noformat
#paper: 12
#mode: syntax
#inlinetest:exclude Not an executable S_H/A example; see TestQuantifiedPaperIndex.
#skip
Surface syntax template; parsed by TestQuantifiedPaperIndex.

-- paper.cue.txt --
forall (A, B) T
exists A { ... }
forall (A: number) func(A, A) -> [A, A]
forall (n in int & >=0) T
