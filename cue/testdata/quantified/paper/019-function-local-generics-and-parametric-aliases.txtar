Paper example 019: Function-local generics and parametric aliases

#paper: 19
#mode: evaluation

-- paper.cue.txt --
map: func<A, B>(func(A) -> B, [...A]) -> [...B]
-- in.cue --
@test(validate, at="map")

map: func<A, B>(func(A) -> B, [...A]) -> [...B]
