// Paper example 075: A relation between input and output records

#noformat
#paper: 75
#mode: evaluation
#inlinetest:exclude Not an executable S_H/A example; see TestQuantifiedPaperIndex.
#skip
Dependent profile D (out of scope).

-- paper.cue.txt --
annotate(A): func(x: A, tag: string) -> {
    value: x
    label: tag
}: {value: x, label: tag}
