// Paper example 094: A symbolic inconsistency exposed by one universal instance

#noformat
#paper: 94
#mode: evaluation

-- paper.cue.txt --
f(A): func(A) -> A
r: f(3) & string
-- in.cue --
@test(validate, conflict, at="r")

f(A): func(A) -> A
r: f(3) & string
