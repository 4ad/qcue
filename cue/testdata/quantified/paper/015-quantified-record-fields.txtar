// Paper example 015: Quantified record fields

#noformat
#paper: 15
#mode: evaluation

-- paper.cue.txt --
r(A): {
    id:   func(A) -> A
    wrap: func(A) -> {value: A}
}
-- in.cue --
@test(validate, at="r")

r(A): {
    id:   func(A) -> A
    wrap: func(A) -> {value: A}
}
