Paper example 070: Matrix dimensions, including empty matrices

#paper: 70
#mode: evaluation
#inlinetest:exclude Not an executable S_H/A example; see TestQuantifiedPaperIndex.
#skip
Dependent profile D (out of scope).

-- paper.cue.txt --
Matrix(A, r in Nat, c in Nat) = {
    rows: r
    cols: c
    data: Vec(Vec(A, c), r)
}
