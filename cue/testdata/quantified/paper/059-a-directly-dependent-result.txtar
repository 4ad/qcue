// Paper example 059: A directly dependent result

#noformat
#paper: 59
#mode: evaluation
#inlinetest:exclude Not an executable S_H/A example; see TestQuantifiedPaperIndex.
#skip
Dependent profile D (out of scope).

-- paper.cue.txt --
successor: func(x: int) -> (x + 1): x + 1

// Equivalent pointwise specification:
successor(n in int): func(n) -> (n + 1)
