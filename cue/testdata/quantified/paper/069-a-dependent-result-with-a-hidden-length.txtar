Paper example 069: A dependent result with a hidden length

#paper: 69
#mode: evaluation
#inlinetest:exclude Not an executable S_H/A example; see TestQuantifiedPaperIndex.
#skip
Dependent profile D (out of scope).

-- paper.cue.txt --
filterBounded(A, n in Nat): func(
    p: func(A) -> bool, xs: Vec(A, n),
) -> (exists (k in int & >=0 & <=n) Vec(A, k)):
    [for x in xs if p(x) {x}]
