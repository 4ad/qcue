-- in.cue --
@test(validate, at="choose")

choose:
	forall (A: int) func(A, A) -> A
choose:
	forall (A: string) func(A, A) -> A

// The stronger declaration also covers mixed unions:
stronger:
	forall (A: int | string) func(A, A) -> A
