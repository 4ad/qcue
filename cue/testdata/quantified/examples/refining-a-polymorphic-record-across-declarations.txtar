-- in.cue --
@test(validate, at="r")

r:
	forall (A) {wrap: func(A) -> {value: A}}
r:
	forall (B) {wrap: func(B) -> {tag: "wrapped"}}

// Unified description, after alpha-renaming:
r:
	forall (T) {wrap: func(T) -> {value: T, tag: "wrapped"}}
