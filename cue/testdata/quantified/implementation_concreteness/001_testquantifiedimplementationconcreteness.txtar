Quantified CUE: ImplementationConcreteness

-- in.cue --
@test(validate, concrete, incomplete, at="unknown")
@test(validate, concrete, at="known")

unknown(A): func(A) -> A
known(A): func(x: A) -> A: x
