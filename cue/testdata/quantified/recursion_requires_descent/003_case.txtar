Quantified CUE: RecursionRequiresDescent/loop: func(xs: [...int], ys: [...int]) -> int: loop(ys, xs)          out: loop([1, 2], [1])

-- in.cue --
@test(validate, conflict, at="out")

loop: func(xs: [...int], ys: [...int]) -> int: loop(ys, xs)
         out: loop([1, 2], [1])
