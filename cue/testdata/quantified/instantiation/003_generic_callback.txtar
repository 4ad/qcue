-- in.cue --
@test(json, [1,1,2,2], at="out")

flatMap:
	forall (A, B) func(g: func(A) -> [...B], xs: [...A]) -> [...B]: [
		for x in xs for y in g(x) {y}
	]
duplicate:
	forall (A) func(x: A) -> [A, A]: [x, x]
out: flatMap(duplicate, [1, 2])
