Quantified CUE: Certification/returned implementation proof

-- in.cue --
@test(validate, at="f")
@test(validate, functions, incomplete, at="f")

f: func() -> (func(int) -> int): func(x: int) -> int: "wrong"
