#skip
#inlinetest:exclude Run by cue/quantified_test.go to exercise Go API export.

-- bad.cue --
import "strings"

f: strings.ToUpper & (func(string) -> "bad")
-- good.cue --
import "strings"

f: strings.ToUpper & (func("a") -> "A")
